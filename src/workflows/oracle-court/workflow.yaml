local-simulation:
  user-workflow:
    workflow-name: "oracle-court"
  workflow-artifacts:
    workflow-path: "./index.ts"
    config-path: "./config.json"

production-testnet:
  user-workflow:
    workflow-name: "oracle-court"
