local-simulation:
  user-workflow:
    workflow-owner-address: "(optional) Multi-signature contract address"
    workflow-name: "block-trigger"
  workflow-artifacts:
    workflow-path: "./index.ts"
    config-path: "./config.json"

production-testnet:
  user-workflow:
    workflow-owner-address: "(optional) Multi-signature contract address"
    workflow-name: "block-trigger"
